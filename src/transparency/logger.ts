// TODO(issue #9): Server-side transparency logger.
// Emits escalation events to structured logs regardless of user-visible mode
// (including transparency: silent). Used for analytics and for the future
// benchmark harness (post-MVP, see brief §11).

export {};
